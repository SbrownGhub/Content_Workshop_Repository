function PdfThumbnail({ fileUrl }) {
  return (
    <div style={{ width: 120, height: 160 }}>
      <Document file={fileUrl}>
        <Page pageNumber={1} width={120} />
      </Document>
    </div>
  );
}